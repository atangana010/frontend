// hooks/usePaiement.js
// Re-export du hook depuis le contexte pour compatibilité
// avec les imports qui viennent de hooks/ plutôt que contexts/
export { usePaiement } from '../contexts/PaiementContext';